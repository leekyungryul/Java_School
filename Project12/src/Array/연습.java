package Array;

import java.util.Arrays;
import java.util.Random;

class calculate{
	public static void swap(int[] arrays, int first, int second) {
		int temp  = arrays[first];
		arrays[first] = arrays[second];
		arrays[second] = temp;
	}

}
